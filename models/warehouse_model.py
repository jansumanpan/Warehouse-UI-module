from openerp import api, fields, models

# class WarehouseTabs(models.Model):
#     _name = 'warehouse.tabs'

#     @api.multi
#     def store_pos_data(self):
#         warehouses = self.env['stock.warehouse'].search([],[])
#         return {'mirror_order': active_mirror_order.id}